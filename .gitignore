# Credentials — never commit real .env files, only .env.example
.env
*.env
!.env.example

# Per-run drill logs (generated by run_all.sh under each topic folder)
run_all_logs/

# Claude Code local project settings (machine-specific permissions/servers)
.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db

# Editor swap/backup files
*.swp
*.swo
*~
